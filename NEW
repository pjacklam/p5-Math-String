This lists only the changes for the current release (v1.14) over the last
version, since HISTORY is now quite big:

* bstr() was not caching the result, missing speedups
* some more tests and some small nits

