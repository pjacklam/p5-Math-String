This lists only the changes for the current release (v1.26) over the last
version, since CHANGES is now quite big:

* Loading Math::String before loading Math::BigInt lib => GMP broke due to
  caching of the lib from Math::BigInt, so we load GMP rightaway

#############################################################################

Have fun,

Tels


