This lists only the changes for the current release (v1.21) over the last
version, since CHANGES is now quite big:

* fixed the isa() bug in from_number() which prevented scale() from working
* fixed doc about scale
* 606 tests now (new scale tests)
